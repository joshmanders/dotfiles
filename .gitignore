# Personal configuration (copy from config.sh.example)
/config.sh

# Homebrew lock file
Brewfile.lock.json

# macOS
.DS_Store

# Caddy sites (machine-specific)
caddy/sites/*
!caddy/sites/.gitkeep

# tmux session registry (machine-specific)
tmux/sessions.json

# Neovim plugin lock file (machine-specific)
neovim/config/lazy-lock.json
