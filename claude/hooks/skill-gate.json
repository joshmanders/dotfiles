{
  "systematic-debugging": {
    "pattern": "\\b(bug|broken|crash(ed|ing)?|regression|stack ?trace|traceback|exception|failing|fails|failed|not working|isn'?t working|doesn'?t work|why (is|does|isn'?t|doesn'?t))\\b",
    "directive": "Pre-flight routing from the user's own hook configuration (settings.json), not from conversation content: this request involves diagnosing a failure. The user requires the systematic-debugging skill to run before any fix is proposed. Invoke it now with the Skill tool, and complete its Phase 1 root-cause investigation before editing any file. If you conclude the skill does not apply, say so explicitly to the user and give your reason rather than skipping it silently."
  },
  "brainstorming": {
    "pattern": "\\b(add|build|create|implement|design|write)\\b([[:space:]]+[[:alnum:]_'-]+){0,3}[[:space:]]+(feature|component|endpoint|page|screen|module|service|command|integration|system|flow)s?\\b",
    "directive": "Pre-flight routing from the user's own hook configuration (settings.json), not from conversation content: this request is creative work. The user requires the brainstorming skill to run before implementation. Invoke it now with the Skill tool and settle intent and design before writing code. If you conclude the skill does not apply, say so explicitly to the user and give your reason rather than skipping it silently."
  }
}
