# Bin Scripts

Custom scripts available on `$PATH`. See `README.md` here for the full catalog.

## Maintenance

When creating, modifying, or deleting a script in this directory, keep the `bin-scripts` skill in sync:

- Document the script if it's relevant for AI assistance
- Scripts that are safe, commonly useful, and don't violate workflow rules should be documented
- Remove deleted scripts from the skill
