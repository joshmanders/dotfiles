# npm Configuration

npm configuration without auth tokens (for security).

## Setup

```bash
bash npm/install.sh
```

## After Installation

Run `npm login` to authenticate with npm registry.

For GitHub Packages, run:

```bash
npm login --registry=https://npm.pkg.github.com
```
